<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.myapp.service.dao.TestDAO">

	<select id="getAllTest" resultType="testVO">
	SELECT * FROM Test
	</select>
	
	<insert id="insertTest">
	INSERT INTO Test(fName, fContent)
	VALUES(#{fName}, #{fContent})
	</insert>
	
	<delete id="deleteTest">
	DELETE FROM Test where fNum=#{fNum}
	</delete>
	

</mapper>